apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sast-ai-frontend-dev
  namespace: sast-ai-prod
  labels:
    app.kubernetes.io/name: sast-ai-frontend-dev
    app.kubernetes.io/part-of: sast-ai
    environment: development
  finalizers:
    - resources-finalizer.argocd.argoproj.io/background
spec:
  project: default
  source:
    repoURL: https://github.com/RHEcosystemAppEng/sast-ai-frontend
    targetRevision: HEAD
    path: deploy/frontend-chart
    helm:
      releaseName: sast-ai-frontend-dev
      valueFiles:
        - values.yaml
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: sast-ai-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  revisionHistoryLimit: 10
