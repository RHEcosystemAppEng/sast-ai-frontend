apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sast-ai-frontend-prod
  namespace: sast-ai-prod
  labels:
    app.kubernetes.io/name: sast-ai-frontend-prod
    app.kubernetes.io/part-of: sast-ai
    environment: production
  finalizers:
    - resources-finalizer.argocd.argoproj.io/background
spec:
  project: default
  source:
    repoURL: https://github.com/RHEcosystemAppEng/sast-ai-frontend
    targetRevision: HEAD
    path: deploy/frontend-chart
    helm:
      releaseName: sast-ai-frontend-prod
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: sast-ai-prod
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m
  revisionHistoryLimit: 10
